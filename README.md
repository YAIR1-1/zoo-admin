**שמות המגישים:** אליהו שנבל — 331237032  
יאיר רוט — 216804237  
אמיתי זהוראי — 209132588  

**שם המערכת:** Zoo Management System

**היחידה הנבחרת מתוך המערכת:** Decision Support & Management Department

---

# מבוא

הפרויקט עוסק בתכנון בסיס נתונים עבור מערכת לניהול גן חיות.  
המערכת נועדה לתמוך בניהול, בקרה וקבלת החלטות על בסיס מידע ארגוני ותפעולי הנשמר במאגר נתונים מסודר.

במסגרת העבודה נבחרה היחידה **Decision Support & Management Department**, שהיא יחידה ניהולית האחראית על מעקב אחר פרויקטים, משימות, דוחות, מדדי ביצוע ובקרות.  
יחידה זו מרכזת נתונים ממספר תחומים בארגון ומאפשרת להנהלה לעקוב אחר פעילות המערכת, לזהות בעיות, למדוד ביצועים ולקבל החלטות מבוססות נתונים.

המערכת שומרת נתונים על מנהלים, פרויקטים, משימות, דוחות, מדדי KPI וביקורות Audit.  
באמצעות נתונים אלו ניתן לבצע פעולות מרכזיות כגון ניהול פרויקטים, שיוך משימות לפרויקטים, הפקת דוחות, מדידת ביצועים וביצוע בקרות ארגוניות.

מטרת התיאור המילולי היא להציג בצורה ברורה את מטרת המערכת, את סוגי הנתונים הנשמרים בה ואת הפונקציונליות המרכזית שנרצה לבצע באמצעותה.

---

# תיאור המערכת

מערכת **Zoo Management System** היא מערכת מידע ארגונית שנועדה לתמוך בפעילות השוטפת והניהולית של גן החיות.  
המערכת שומרת מידע ממספר תחומים שונים בארגון ומאפשרת עבודה מסודרת, מעקב אחר תהליכים, ותמיכה בתהליכי קבלת החלטות.

בשלב זה של הפרויקט התמקדנו באגף ניהולי פנימי בשם:

**Decision Support & Management Department**

אגף זה משמש שכבת בקרה וניהול, ותפקידו לאסוף מידע, לנתח נתונים ולהפיק תובנות ניהוליות.

---

# היחידה הנבחרת

היחידה שנבחרה מתוך המערכת היא **Decision Support & Management Department**.

אגף זה אחראי על:

- ניהול פרויקטים ארגוניים
- מעקב אחר משימות
- הפקת דוחות ניהוליים
- מדידת ביצועים באמצעות KPI
- ביצוע ביקורות ובקרות Audit
- תמיכה בקבלת החלטות ניהוליות

היחידה מייצגת שכבה ניהולית חשובה במערכת, משום שהיא אינה מטפלת רק בתפעול היומיומי אלא גם בניתוח, בקרה ותכנון.

---

# הנתונים הנשמרים במערכת

במסגרת היחידה הנבחרת נשמרים הנתונים הבאים:

## 1. מנהלים (Manager)
המערכת שומרת מידע על מנהלים, כולל:
- מזהה מנהל
- שם פרטי
- שם משפחה
- אימייל
- טלפון
- תאריך קליטה
- רמת ותק

## 2. פרויקטים (Project)
עבור כל פרויקט נשמרים:
- מזהה פרויקט
- שם פרויקט
- תיאור
- תאריך התחלה
- תאריך סיום
- תקציב
- סטטוס
- שיוך למנהל אחראי

## 3. משימות (Task)
עבור כל משימה נשמרים:
- מזהה משימה
- שם משימה
- תיאור
- תאריך יעד
- עדיפות
- סטטוס
- שיוך לפרויקט

## 4. דוחות (Report)
המערכת שומרת דוחות הכוללים:
- מזהה דוח
- שם דוח
- תאריך דוח
- שם מחלקה
- סוג דוח
- תקציר
- המנהל שיצר את הדוח

## 5. מדדי KPI
עבור כל מדד נשמרים:
- מזהה KPI
- שם המדד
- שם מחלקה
- ערך יעד
- ערך בפועל
- תאריך מדידה
- יחידת מדידה
- שיוך לפרויקט

## 6. ביקורות Audit
עבור כל ביקורת נשמרים:
- מזהה ביקורת
- תאריך ביקורת
- שם מחלקה
- ממצא
- דרגת חומרה
- סטטוס
- שיוך למנהל
- שיוך לפרויקט

---

# פונקציונליות עיקרית

הפונקציונליות המרכזית שנרצה לבצע במערכת כוללת:

- ניהול פרויקטים ומעקב אחר סטטוס הפרויקט
- שיוך משימות לפרויקטים ומעקב אחר תאריך יעד ועדיפות
- שיוך מנהלים לפרויקטים, דוחות וביקורות
- הפקת דוחות ניהוליים מתוך נתוני המערכת
- שמירת מדדי KPI לצורך מדידת ביצועים
- ניתוח פער בין ערך יעד לערך בפועל במדדי ביצוע
- ביצוע ביקורות Audit ותיעוד ממצאים
- תמיכה בקבלת החלטות על בסיס נתונים מעודכנים

המערכת מאפשרת מעבר ממערכת תפעולית פשוטה למערכת מידע ניהולית מלאה, שבה הנתונים אינם רק נשמרים אלא גם משמשים לבקרה, ניתוח ותכנון.

---

# מבנה נתונים וממשק משתמש (Mockups)

להלן תרשים מבנה הנתונים וצילומי מסך המדגימים את הממשק העתידי של מערכת ניהול גן החיות:

###**תרשים ERD**:
![תרגשים ERD](https://github.com/YAIR1-1/zoo-admin/blob/main/331237032_216804237_209132588/stage1/erd.png)

### תרשים מבנה נתונים (DSD)
![תרשים DSD](https://github.com/YAIR1-1/zoo-admin/blob/main/331237032_216804237_209132588/stage1/DSD.png)

### מסך ראשי (Dashboard)
![לוח בקרה](https://github.com/YAIR1-1/zoo-admin/blob/main/331237032_216804237_209132588/stage1/site/dashboard.png)

### ניהול פרויקטים
![פרויקטים](https://github.com/YAIR1-1/zoo-admin/blob/main/331237032_216804237_209132588/stage1/site/projects.png)

### דוחות
![דוחות](https://github.com/YAIR1-1/zoo-admin/blob/main/331237032_216804237_209132588/stage1/site/reports.png)

### מערכת בקרה
![בקרה](https://github.com/YAIR1-1/zoo-admin/blob/main/331237032_216804237_209132588/stage1/site/control.png)




# שלב ב — שאילתות ואילוצים

## תיאור כללי

בשלב זה ביצענו תשאול של בסיס הנתונים של מערכת ניהול הפרויקטים.
כתבנו שאילתות SELECT, UPDATE ו-DELETE ברמת מורכבות גבוהה, כולל שאילתות מקוננות, שימוש בתאריכים, GROUP BY ו-ORDER BY.

---

## קבצים

| קובץ | תיאור |
|------|--------|
| `Queries.sql` | 8 שאילתות SELECT, 3 UPDATE, 3 DELETE |
| `Constraints.sql` | 3 אילוצים חדשים |
| `RollbackCommit.sql` | דוגמאות Rollback ו-Commit |
| `Index.sql` | 3 אינדקסים עם בדיקת זמני ריצה |
| `backup2` | גיבוי מעודכן של בסיס הנתונים |

---

## שאילתות SELECT

### שאילתה 1א — מנהלים עם משימות פתוחות (JOIN + DISTINCT)

**תיאור:** מציאת כל המנהלים שיש להם פרויקטים עם משימות שטרם הושלמו.

```sql
SELECT DISTINCT m.first_name, m.last_name, p.project_name
FROM manager m
JOIN project p ON m.manager_id = p.manager_id
JOIN task t ON p.project_id = t.project_id
WHERE t.status != 'Completed';
```

---

### שאילתה 1ב — אותה תוצאה עם EXISTS (גרסה שנייה)

```sql
SELECT m.first_name, m.last_name, p.project_name
FROM manager m
JOIN project p ON m.manager_id = p.manager_id
WHERE EXISTS (
    SELECT 1 
    FROM task t 
    WHERE t.project_id = p.project_id 
    AND t.status != 'Completed'
);
```

**הבדל בין שתי הגרסאות:**

| | גרסה 1א — JOIN + DISTINCT | גרסה 1ב — EXISTS |
|--|--------------------------|-------------------|
| **איך עובד** | מצרף את כל השורות ואז מסנן כפילויות | עוצר ברגע שמוצא שורה אחת תואמת |
| **יעילות** | פחות יעיל — צריך לעבור על כל השורות ולהסיר כפילויות | יותר יעיל — EXISTS מפסיק לחפש ברגע שמוצא תוצאה ראשונה |
| **מתי עדיף** | כשצריך שדות מטבלת ה-JOIN | כשרק צריך לבדוק קיום |

**מסקנה:** גרסה 1ב יעילה יותר כי EXISTS משתמש ב-short-circuit evaluation.

---

### שאילתה 2א — פרויקטים השנה עם משימות מאוחרות (תת-שאילתה)

**תיאור:** פרויקטים שהתחילו השנה, עם ספירת המשימות שעברו את תאריך היעד ולא הושלמו.

```sql
SELECT 
    p.project_name,
    EXTRACT(MONTH FROM p.start_date) AS start_month,
    (SELECT COUNT(*) 
     FROM task t 
     WHERE t.project_id = p.project_id 
     AND t.due_date < CURRENT_DATE 
     AND t.status != 'Completed') AS delayed_tasks_count
FROM project p
WHERE EXTRACT(YEAR FROM p.start_date) = EXTRACT(YEAR FROM CURRENT_DATE);
```

---

### שאילתה 2ב — אותה תוצאה עם LEFT JOIN + GROUP BY (גרסה שנייה)

```sql
SELECT 
    p.project_name,
    EXTRACT(MONTH FROM p.start_date) AS start_month,
    COUNT(t.task_id) AS delayed_tasks_count
FROM project p
LEFT JOIN task t ON p.project_id = t.project_id 
    AND t.due_date < CURRENT_DATE 
    AND t.status != 'Completed'
WHERE EXTRACT(YEAR FROM p.start_date) = EXTRACT(YEAR FROM CURRENT_DATE)
GROUP BY p.project_id, p.project_name, EXTRACT(MONTH FROM p.start_date);
```

**הבדל בין שתי הגרסאות:**

| | גרסה 2א — תת-שאילתה | גרסה 2ב — LEFT JOIN + GROUP BY |
|--|---------------------|--------------------------------|
| **איך עובד** | לכל שורה ב-project מריץ שאילתה נפרדת | מצרף הכל בפעולה אחת ומקבץ |
| **יעילות** | פחות יעיל — N שאילתות (אחת לכל פרויקט) | יותר יעיל — פעולה אחת על כל הנתונים |
| **מתי עדיף** | כשהתת-שאילתה מורכבת ותלויה בהקשר | כשיש אינדקס על עמודות ה-JOIN |

**מסקנה:** גרסה 2ב יעילה יותר כי מבצעת JOIN אחד במקום N שאילתות.

---

## שאילתות UPDATE

### UPDATE 1 — קידום מנהלים ותיקים

**תיאור:** כל מנהל שהצטרף לפני 2023 ועדיין מסווג כ-Junior מקודם ל-Senior.

```sql
UPDATE manager 
SET seniority_level = 'Senior' 
WHERE hire_date < '2023-01-01' AND seniority_level = 'Junior';
```

בדיקה לפני:
```sql
SELECT manager_id, first_name, last_name, hire_date, seniority_level 
FROM manager 
WHERE hire_date < '2023-01-01' AND seniority_level = 'Junior';
```

---

### UPDATE 2 — סגירת משימות בפרויקטים שהסתיימו

**תיאור:** כל משימה השייכת לפרויקט שסטטוסו Completed — מסומנת גם היא כ-Completed.

```sql
UPDATE task 
SET status = 'Completed' 
WHERE project_id IN (
    SELECT project_id FROM project WHERE status = 'Completed'
);
```

בדיקה לפני:
```sql
SELECT task_id, task_name, status, project_id 
FROM task 
WHERE project_id IN (SELECT project_id FROM project WHERE status = 'Completed');
```

---

### UPDATE 3 — הפחתת תקציב לפרויקטים עם ביקורות חמורות

**תיאור:** פרויקטים שקיבלו ביקורת בחומרה גבוהה (High) — תקציבם מופחת ב-10%.

```sql
UPDATE project 
SET budget = budget * 0.9 
WHERE project_id IN (
    SELECT project_id FROM audit WHERE severity = 'High'
);
```

בדיקה לפני:
```sql
SELECT project_id, project_name, budget 
FROM project 
WHERE project_id IN (SELECT project_id FROM audit WHERE severity = 'High');
```

---

## שאילתות DELETE

### DELETE 1 — מחיקת דוחות ישנים

**תיאור:** מחיקת כל הדוחות שנוצרו לפני שנת 2024.

```sql
DELETE FROM report 
WHERE report_date < '2024-01-01';
```

בדיקה לפני:
```sql
SELECT report_id, report_name, report_date 
FROM report 
WHERE report_date < '2024-01-01';
```

---

### DELETE 2 — מחיקת ביקורות סגורות בחומרה נמוכה

**תיאור:** מחיקת ביקורות שסגורות וחומרתן נמוכה — אין בהן עוד צורך.

```sql
DELETE FROM audit 
WHERE status = 'Closed' AND severity = 'Low';
```

בדיקה לפני:
```sql
SELECT audit_id, department_name, severity, status 
FROM audit 
WHERE status = 'Closed' AND severity = 'Low';
```

---

### DELETE 3 — מחיקת משימות ללא פרויקט פעיל

**תיאור:** מחיקת משימות השייכות לפרויקטים שעדיין בסטטוס Pending ומתחילות לפני 2024 — פרויקטים שלא יצאו לפועל.

```sql
DELETE FROM task 
WHERE project_id IN (
    SELECT project_id FROM project 
    WHERE status = 'Pending' AND start_date < '2024-01-01'
);
```

בדיקה לפני:
```sql
SELECT task_id, task_name, project_id 
FROM task 
WHERE project_id IN (
    SELECT project_id FROM project 
    WHERE status = 'Pending' AND start_date < '2024-01-01'
);
```

---

## Rollback ו-Commit

### Rollback — ביטול עדכון

```sql
BEGIN;

UPDATE manager 
SET seniority_level = 'Senior' 
WHERE seniority_level = 'Junior';

-- בדיקת מצב אחרי העדכון
SELECT manager_id, first_name, last_name, seniority_level FROM manager;

ROLLBACK;

-- בדיקת מצב אחרי הביטול — חזרה למצב המקורי
SELECT manager_id, first_name, last_name, seniority_level FROM manager;
```

### Commit — שמירת עדכון

```sql
BEGIN;

UPDATE project 
SET status = 'Completed' 
WHERE end_date < CURRENT_DATE AND status != 'Completed';

-- בדיקת מצב אחרי העדכון
SELECT project_id, project_name, status, end_date FROM project;

COMMIT;

-- בדיקת מצב אחרי ה-Commit — השינוי נשמר
SELECT project_id, project_name, status, end_date FROM project;
```

---

## אילוצים (Constraints)

### אילוץ 1 — תקציב חיובי

```sql
ALTER TABLE project 
ADD CONSTRAINT chk_budget_positive 
CHECK (budget > 0);
```

**תיאור:** מונע הכנסת פרויקט עם תקציב שלילי או אפס.

---

### אילוץ 2 — תאריך סיום אחרי תאריך התחלה

```sql
ALTER TABLE project 
ADD CONSTRAINT chk_end_after_start 
CHECK (end_date > start_date);
```

**תיאור:** מונע הכנסת פרויקט שתאריך הסיום שלו קודם לתאריך ההתחלה.

---

### אילוץ 3 — ערכי seniority_level חוקיים

```sql
ALTER TABLE manager 
ADD CONSTRAINT chk_seniority_level 
CHECK (seniority_level IN ('Junior', 'Mid', 'Senior'));
```

**תיאור:** מונע הכנסת ערך לא חוקי לשדה רמת הבכירות.

---

## אינדקסים (Indexes)

### אינדקס 1 — על project.manager_id

```sql
CREATE INDEX idx_project_manager 
ON project(manager_id);
```

**מוטיבציה:** שאילתות JOIN בין manager ל-project רצות לעיתים קרובות. האינדקס מייעל את החיפוש.

---

### אינדקס 2 — על task.project_id

```sql
CREATE INDEX idx_task_project 
ON task(project_id);
```

**מוטיבציה:** שאילתות שמחפשות משימות לפי פרויקט נפוצות מאוד. האינדקס מאיץ את הסינון.

---

### אינדקס 3 — על task.due_date

```sql
CREATE INDEX idx_task_due_date 
ON task(due_date);
```

**מוטיבציה:** שאילתות שמחפשות משימות מאוחרות לפי תאריך יעד נהנות מהאינדקס כי הן מסננות לפי טווח תאריכים.

---

### בדיקת זמני ריצה

לפני הוספת אינדקס:
```sql
EXPLAIN ANALYZE
SELECT * FROM task 
WHERE due_date < CURRENT_DATE AND status != 'Completed';
```

אחרי הוספת אינדקס:
```sql
CREATE INDEX idx_task_due_date ON task(due_date);

EXPLAIN ANALYZE
SELECT * FROM task 
WHERE due_date < CURRENT_DATE AND status != 'Completed';
```

**הסבר:** לפני האינדקס PostgreSQL מבצע Seq Scan — סורק את כל הטבלה. אחרי האינדקס מבצע Index Scan — קופץ ישירות לשורות הרלוונטיות. ההבדל בזמן ניכר ככל שהטבלה גדולה יותר.



## שלב ג — אינטגרציה ומבטים

## תיאור כללי

בשלב זה ביצענו אינטגרציה בין שתי מערכות:
- **המערכת שלנו** — מערכת ניהול פרויקטים (Manager, Project, Task, Report, KPI, Audit)
- **המערכת שקיבלנו** — מערכת כרטיסים ומבקרים (Visitors, Employees, Transactions, Ticket_Types, Transaction_Items, Memberships)

---

## קבצים

| קובץ | תיאור |
|------|--------|
| `Integrate.sql` | יצירת טבלאות, שינוי טבלאות קיימות, קשרי אינטגרציה, הכנסת נתונים |
| `Views.sql` | 3 מבטים + 2 שאילתות לכל מבט |
| `ERD_Combined.erdplus` | תרשים ERD המשולב |

---

## הינדוס לאחור (Reverse Engineering)

קיבלנו גיבוי של המערכת השנייה והרצנו אותו בסביבה המקומית. מתוך הטבלאות שנוצרו בנינו את ה-ERD לפי האלגוריתם הבא:

1. כל טבלה הפכה לישות (Entity)
2. כל עמודה רגילה הפכה לתכונה (Attribute)
3. עמודת PRIMARY KEY — תכונה מזהה (עם קו תחתי)
4. עמודת FOREIGN KEY — קשר (Relationship) בין שתי ישויות
5. טבלה עם שני FK — קשר רבים-לרבים (M:N)
6. NOT NULL — השתתפות מלאה בקשר

---

## החלטות האינטגרציה

### קשר 1 — Manager supervises Employees (1:N)
**החלטה:** שתי המערכות שייכות לאותו ארגון. ה-`Manager` ממערכת ניהול הפרויקטים מפקח על ה-`Employees` ממערכת הכרטיסים.

**יישום:**
```sql
ALTER TABLE Employees ADD COLUMN manager_ID INT;
ALTER TABLE Employees ADD CONSTRAINT fk_emp_manager
    FOREIGN KEY (manager_ID) REFERENCES manager(manager_id);
```

### קשר 2 — Manager approves Transactions (1:N)
**החלטה:** כל עסקה במערכת הכרטיסים טעונה אישור מנהל מהמערכת הראשונה — מה שמקשר בין שתי המערכות עסקית.

**יישום:**
```sql
ALTER TABLE Transactions ADD COLUMN Manager_ID INT;
ALTER TABLE Transactions ADD CONSTRAINT fk_trans_manager
    FOREIGN KEY (Manager_ID) REFERENCES manager(manager_id);
```

### שינויים נוספים בטבלאות קיימות
```sql
ALTER TABLE users RENAME TO Employees;
ALTER TABLE Employees RENAME COLUMN id TO Employee_ID;
ALTER TABLE tickets RENAME TO Transactions;
ALTER TABLE Transactions RENAME COLUMN ticketid TO Transaction_ID;
```

---

## מבטים (Views)

### מבט 1 — view_manager_projects
מנקודת המבט של המערכת שלנו. מציג מנהל + הפרויקטים שלו + המשימות.

```sql
CREATE VIEW view_manager_projects AS
SELECT m.manager_id, m.first_name || ' ' || m.last_name AS manager_name,
       m.seniority_level, p.project_id, p.project_name,
       p.status AS project_status, p.budget,
       t.task_id, t.task_name, t.priority, t.status AS task_status
FROM manager m
JOIN project p ON p.manager_id = m.manager_id
JOIN task    t ON t.project_id  = p.project_id;
```

**שאילתה 1** — פרויקטים פעילים ממוינים לפי תקציב:
```sql
SELECT manager_name, project_name, budget
FROM view_manager_projects
WHERE project_status = 'Active'
ORDER BY budget DESC;
```

**שאילתה 2** — כמה משימות בעדיפות גבוהה לכל מנהל:
```sql
SELECT manager_name, COUNT(*) AS high_priority_tasks
FROM view_manager_projects
WHERE task_status = 'High'
GROUP BY manager_name
ORDER BY high_priority_tasks DESC;
```

---

### מבט 2 — view_visitor_purchases
מנקודת המבט של המערכת השנייה. מציג מבקר + העסקאות שלו + סוגי הכרטיסים שקנה.

```sql
CREATE VIEW view_visitor_purchases AS
SELECT v.visitorid, v.first_name || ' ' || v.last_name AS visitor_name,
       v.email, tr.Transaction_ID, tr.Transaction_Date,
       tr.Payment_Method, tr.Total_Amount,
       tt.Ticket_Name, tt.Category, ti.Quantity, ti.Price_At_Sale
FROM visitors v
JOIN Transactions      tr ON tr.Visitor_ID     = v.visitorid
JOIN Transaction_Items ti ON ti.Transaction_ID = tr.Transaction_ID
JOIN Ticket_Types      tt ON tt.Ticket_ID      = ti.Ticket_ID;
```

**שאילתה 1** — סך הקניות של כל מבקר:
```sql
SELECT visitor_name, COUNT(Transaction_ID) AS num_transactions,
       SUM(Total_Amount) AS total_spent
FROM view_visitor_purchases
GROUP BY visitor_name
ORDER BY total_spent DESC;
```

**שאילתה 2** — איזה סוג כרטיס הכי פופולרי:
```sql
SELECT Ticket_Name, Category, SUM(Quantity) AS total_sold
FROM view_visitor_purchases
GROUP BY Ticket_Name, Category
ORDER BY total_sold DESC;
```

---

### מבט 3 — view_transaction_approval (מבט משולב)
מבט המשלב את שתי המערכות. מציג עסקאות + המנהל שאישר + העובד שביצע.

```sql
CREATE VIEW view_transaction_approval AS
SELECT tr.Transaction_ID, tr.Transaction_Date,
       tr.Payment_Method, tr.Total_Amount,
       e.First_Name || ' ' || e.Last_Name   AS employee_name,
       m.first_name || ' ' || m.last_name   AS approving_manager,
       m.seniority_level,
       v.first_name || ' ' || v.last_name   AS visitor_name
FROM Transactions tr
JOIN Employees e ON e.Employee_ID = tr.Employee_ID
JOIN manager   m ON m.manager_id  = tr.Manager_ID
JOIN visitors  v ON v.visitorid   = tr.Visitor_ID;
```

**שאילתה 1** — סך העסקאות שכל מנהל אישר:
```sql
SELECT approving_manager, COUNT(*) AS total_approved,
       SUM(Total_Amount) AS total_amount
FROM view_transaction_approval
GROUP BY approving_manager
ORDER BY total_amount DESC;
```

**שאילתה 2** — עסקאות מעל 100 ש"ח עם פרטי המנהל והעובד:
```sql
SELECT Transaction_Date, Total_Amount, employee_name,
       approving_manager, visitor_name
FROM view_transaction_approval
WHERE Total_Amount > 100
ORDER BY Transaction_Date;
```

---

## בדיקת נתונים

```sql
SELECT 'audit'             AS table_name, COUNT(*) AS row_count FROM audit
UNION ALL SELECT 'kpi',             COUNT(*) FROM kpi
UNION ALL SELECT 'manager',         COUNT(*) FROM manager
UNION ALL SELECT 'project',         COUNT(*) FROM project
UNION ALL SELECT 'report',          COUNT(*) FROM report
UNION ALL SELECT 'task',            COUNT(*) FROM task
UNION ALL SELECT 'visitors',        COUNT(*) FROM visitors
UNION ALL SELECT 'Employees',       COUNT(*) FROM Employees
UNION ALL SELECT 'Transactions',    COUNT(*) FROM Transactions
UNION ALL SELECT 'Transaction_Items',COUNT(*) FROM Transaction_Items
UNION ALL SELECT 'Ticket_Types',    COUNT(*) FROM Ticket_Types
UNION ALL SELECT 'Memberships',     COUNT(*) FROM Memberships;
```

